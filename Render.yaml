services:
  - type: web
    name: arkheia-cps-backend
    env: python
    plan: free

    buildCommand: "pip install -r requirements.txt"

    startCommand: "uvicorn arkheia_cps.app.main:app --host 0.0.0.0 --port 8000"

    autoDeploy: true
